# 统一换行符，避免 Windows 本地（CRLF）与 Linux CI（LF）之间产生巨量假 diff
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd 可能解析异常
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
